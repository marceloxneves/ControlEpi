@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        @*<ul class="nav navbar-nav navbar-right">
                <li>
                    @Html.ActionLink("Olá " + User.Identity.GetUserName().Substring(0, User.Identity.GetUserName().IndexOf("@", System.StringComparison.Ordinal)) + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Configuração" })
                </li>
                <li title="Sair do Sistema"><a href="javascript:document.getElementById('logoutForm').submit()">Sair</a></li>
            </ul>*@
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    @*<span class="glyphicon glyphicon-user"></span>*@
                    <strong>@User.Identity.GetUserName().Substring(0, User.Identity.GetUserName().IndexOf("@", StringComparison.Ordinal))</strong>
                    <span class="glyphicon glyphicon-chevron-down"></span>
                </a>
                <ul class="dropdown-menu col-lg-6">
                    <li>
                        <div class="row ">
                            <div class="col-lg-10 col-lg-offset-2">
                                <a href="@Url.Action("Index","Manage")" class="col-lg-12" style="text-decoration:none;">
                                    <span class="glyphicon glyphicon-cog"></span> Config.
                                </a>
                            </div>

                        </div>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <div class="row">
                            <div class="col-lg-8 col-lg-offset-2">
                                <a href="javascript:document.getElementById('logoutForm').submit()" class="col-lg-12 " style="text-decoration:none;">
                                    <span class="glyphicon glyphicon-log-out"></span> Sair
                                </a>
                            </div>
                        </div>
                    </li>
                </ul>
            </li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Registrar", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Entrar", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}